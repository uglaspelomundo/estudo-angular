import { ElementErrorDirective } from './element-error.directive';

describe('ElementErrorDirective', () => {
  it('should create an instance', () => {
    const directive = new ElementErrorDirective();
    expect(directive).toBeTruthy();
  });
});
